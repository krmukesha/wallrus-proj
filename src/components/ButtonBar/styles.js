const styles = (theme) => ({
  primary: {
    marginRight: theme.spacing(2),
  },
  secondary: {
    background: theme.palette.secondary["100"],
    color: "white",
  },
  spaceTop: {
    marginTop: 20,
  },
});
export default styles;
