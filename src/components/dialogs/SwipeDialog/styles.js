const styles = (theme) => ({
  container: {
    maxWidth: 600,
    flexGrow: 1,
    textAlign: "center",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
  },
  stepsContainer: {
    marginLeft: 72,
    textAlign: "left",
    marginTop: 20,
    height: 65,
  },
  bottomMargin: {
    marginBottom: theme.spacing(2),
  },
});
export default styles;
